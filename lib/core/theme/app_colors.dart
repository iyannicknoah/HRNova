import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Gradient Brand Colors (Soft rich teals/emerald)
  static const Color primaryGreen = Color(0xFF0F766E);
  static const Color darkGreen = Color(0xFF0F766E); // Match primary
  static const Color lightGreen = Color(0xFF14B8A6);

  // Background and Cards (Deep elegant slate colors)
  static const Color darkNavy = Color(0xFF0F172A);
  static const Color cardNavy = Color(0xFF1E293B);

  // Accent sky-blues and deep indigo/slate replacements for warning/error
  static const Color warningAmber = Color(0xFF6366F1); // Indigo (Warning replacement)
  static const Color errorRed = Color(0xFF64748B);     // Slate Grey (Error/Suspended replacement)
  static const Color infoBlue = Color(0xFF0EA5E9);     // Sky Blue

  // Status Badges (Pills)
  static const Color pillGreenBg = Color(0xFFE2F9F3);
  static const Color pillGreenText = Color(0xFF0D9488);
  
  static const Color pillAmberBg = Color(0xFFEEF2F6);  // Soft slate blue
  static const Color pillAmberText = Color(0xFF4F46E5); // Indigo
  
  static const Color pillRedBg = Color(0xFFF1F5F9);    // Soft light slate
  static const Color pillRedText = Color(0xFF475569);   // Slate Grey
  
  static const Color pillBlueBg = Color(0xFFE0F2FE);
  static const Color pillBlueText = Color(0xFF0284C7);
}
