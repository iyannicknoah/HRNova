import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('HRNova placeholder test', (WidgetTester tester) async {
    // Full app requires Firebase — tested via integration tests
    expect(true, isTrue);
  });
}
